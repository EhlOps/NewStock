# Build a clean context — the venv is recreated inside the image by `uv sync`.
.venv
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

.git
.gitignore
.dockerignore
Dockerfile
docker-compose.yaml

# Don't bake secrets into the image (they're mounted at runtime instead).
gcloud.json
